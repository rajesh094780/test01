name: Oracle DB Refresh - UAT
on:
  workflow_dispatch:
    inputs:
      customercode:
        description: "CUSTOMER CODE"
        required: true
jobs:
  GET_DETAILS_FROM_AZURE:
    runs-on: ubuntu-latest #veuwcore1120 #windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Run - If Steps/Job Fails"
        run: |
          pwd
          python3 --version
          cd "src/dbrefresh/"
          python3 test.py
          
